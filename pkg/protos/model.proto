syntax = "proto3";

package model;  

option go_package = "pkg/protos";  

service VideoOutput {
    rpc OutputVideoStream (stream Request) returns (Response) {}
}

message Request {
    bytes frame = 1;  // serialized bitmap with shape (height, width, 3)
}

message Response {
}